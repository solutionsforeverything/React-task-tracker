
const Tasks = ({tasks}) => {
   
    return (
        <>
            {
                tasks.map((task)=>(
                    <Task key={}></Task>
                ))
            }
        </>
    )
}

export default Tasks
