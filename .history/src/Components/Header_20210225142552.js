import PropTypes from 'prop-types';

const Header = ({title}) => {
    return ( 
        <header className="app-header">
            <h1>{title}</h1>
        </header>
     );
}



Header.propTypes={
title:PropTypes.striGreeting.defaultProps = {
  name: 'Stranger'
};
ng,
}
export default Header;